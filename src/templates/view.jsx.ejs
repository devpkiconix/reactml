// Code generated by reactml
import React from 'react';
import { connect } from 'react-redux';
// eslint-disable-next-line
import { withStyles } from '@iconix/reactml/node_modules/@material-ui/core/styles';


// eslint-disable-next-line
import lib from '../actionLib';

<%- tagImport.trim() %>

// eslint-disable-next-line
const stateNodeName = <%- JSON.stringify(component.stateNodeName) %>;
const <%-compName %> = (props) => (<%- reactComponentBody %>);

<% if (mapStateToProps) { %>
const mapStateToProps = ({ reactml }) => {
    let compState = reactml.get(stateNodeName);
    return <%- mapStateToProps %>;
};
<% } else { %>
const mapStateToProps = null;
<% }  %>
const mapActionsToProps =
    <%- mapActionsToProps %>;

const stylish = withStyles(<%-JSON.stringify(styles)%>);
export default connect(mapStateToProps, mapActionsToProps)(stylish(<%- compName %>));
