// Code generated by reactml
import React from "react";
import connect from "react-redux";

import lib from "./lib";

import TagFactory from "./tag-factory";
const { Link } = TagFactory;

const Header = props => (
  <div className={props.classes.root}>
    <div>
      <Link to={"/reactml/view"}>home</Link>

      <span>|</span>

      <Link to={"/reactml/editor"}>editor</Link>

      <span>|</span>

      <Link to={"/reactml/about"}>about</Link>
    </div>
  </div>
);

const mapStateToProps = ({ reactml }) => {
  let compState = reactml.get(stateNodeName);
  return {};
};

const mapActionsToProps = null;

export default connect(
  mapStateToProps,
  mapActionsToProps
)(Header);
